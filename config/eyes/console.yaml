# 控制台 eye 配置（本地调试用的眼睛）
#
# 眼层配置约定：每个平台（eye）的配置由自己的 msgspec schema 定义
# （本文件对应 gensokyoai/satori/perceiver.py 的 ConsoleEyeSettings）。
# 单文件平台用 config/eyes/{平台名}.yaml；多文件平台（如 nb2 要吃自己的
# .env）用 config/eyes/{平台名}/ 目录，由 eye 自己解析。
sender: "你"
