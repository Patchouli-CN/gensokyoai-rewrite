
# 维护者规范文件

## 代码规范

- 符合 PEP8 标准
- 类型提示请使用 py310+ 语法，不允许使用 typing.Dict / List / Set 等老旧 typing
- 空注解请用联合类型，比如 `str | None` 而不是 `Optional[str]`
- 不允许函数内导入，如果出现循环导入说明架构不正确，应修架构而非破例
- 不允许过度封装 / 过度工程化的代码，比如过度防御
- 函数必须写明详细的文档字符串（含参数说明、返回值说明、异常说明）

## 提交规范

- 遵循 Conventional Commits 格式：`type: subject`
  - `feat` / `fix` / `refactor` / `docs` / `test` / `chore`
- 每个 commit 只做一件事，不要混改
- AI写的代码，提交请标注`(AI提交: <模型名字>)`，方便审计

## 测试要求

- 新代码必须包含对应测试，`pytest` 全绿才能提 PR
- 测试命名：`test_<功能名>`
- 核心逻辑覆盖率不低于 80%

## 文档同步

- 代码改动涉及接口/行为变更时，必须同步更新架构文档
- 新增模块必须在 `__init__.py` 中暴露公共 API
- 其他功能或描述文档请放顶层`docs/`目录下

## 其他

- CI 必须全绿
- 提交 PR 前请先 fork 并创建特性分支
- 尽量不要提交`ai slop`式的代码，可以用AI开发但代码必须高质量
- 贡献即代表同意以 [LICENSE] 授权
